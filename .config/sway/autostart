#!/bin/sh

run() {
  if ! pgrep "$1" >/dev/null; then
   "$@"&
  fi
}

run pipewire
run pipewire-pulse

run dunst

run blueman-applet

# run emacs -bg "#17191a" --daemon -fs

# run aria2c --rpc-secret="${RPC_SECRET}" -D
