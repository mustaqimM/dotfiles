# -*- conf -*-

include dark

# Window decoration
# class                 border  backgr. text    indicator child_border
client.focused          $base01 $base00 $base07 $base0D $base02
client.focused_inactive $base01 $base01 $base05 $base03 $base01
client.unfocused        $base01 $base00 $base05 $base01 $base01
client.urgent           $base08 $base08 $base00 $base08 $base08
client.placeholder      $base00 $base00 $base05 $base00 $base00
client.background       $base07

# Title format for windows
for_window [shell="xdg_shell"] title_format "%title (%app_id)"
for_window [shell="x_wayland"] title_format "%class - %title"

# Borders
default_border pixel 1
default_floating_border pixel 1
smart_gaps on
hide_edge_borders --i3 both

# Hide titlebar on tabbed windows
font SF Pro Text 0
titlebar_border_thickness 0
titlebar_padding 0

# theme
# set $gnome-schema org.gnome.desktop.interface
# exec_always {
#     gsettings set $gnome-schema gtk-theme 'WhiteSur-dark-solid'
#     gsettings set $gnome-schema icon-theme 'Papirus-Dark'
#     gsettings set $gnome-schema cursor-theme 'X11-Cursors-Breeze-Hacked-White'
#     gsettings set $gnome-schema font-name 'SF Pro Text 11'
# }

# set $cursor Breeze-Hacked-White
# seat * xcursor_theme $cursor
seat * hide_cursor when-typing enable
