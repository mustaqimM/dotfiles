#compdef switch_theme

_switch_theme() {
  local state

  _arguments \
    '1: :->theme_profile'

  case $state in
    (theme_profile) _arguments '1:Select an option:(light dark toggle)' ;;
  esac
}

