#!/bin/zsh

decode() {
    declare string
    declare base

    if [ "$XDG_SESSION_TYPE" = "wayland" ];then
        base=$(wl-paste)
    else
        base=$(xclip -sel clip -o)
    fi

    string=$(base64 -d <<< $(echo $base | awk '{$1=$1};1'))

    wl-copy $string
    echo $string
}

decode "$@"
